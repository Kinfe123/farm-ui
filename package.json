{
  "name": "farmui",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@6.14.1",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "api:build": "dotenv -- turbo --filter api build",
    "web:build": "dotenv -- turbo --filter web build",
    "api:dev": "dotenv -- turbo --filter api dev",
    "web:dev": "dotenv -- turbo --filter web dev",
    "test": "turbo test"
  },
  "dependencies": {
    "@changesets/changelog-github": "^0.4.8",
    "@changesets/cli": "^2.29.4",
    "dotenv-cli": "^7.4.4",
    "vite-tsconfig-paths": "^4.3.2",
    "vitest": "^0.31.4"
  }
}
